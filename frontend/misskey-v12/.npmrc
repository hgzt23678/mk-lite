save-exact = true
# Production builds use `npm ci`; the dependency graph must be immutable and
# reviewable. Upstream Misskey v12 used Yarn and disabled npm's lockfile, but
# this port intentionally ships package-lock.json.
package-lock = true
