<MkCapabilityUnavailable Capability="@Capability"
                           Reason="The current Dolphin contract has no ranked, pinned, or recently active users endpoint for this v12 view." />

@code {
    [Parameter]
    public string Capability { get; set; } = "explore/users";
}
