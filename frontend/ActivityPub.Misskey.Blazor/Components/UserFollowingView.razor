@if (Pagination is not null)
{
    <MkUserFollowList Pagination="Pagination" />
}

@code {
    [Parameter, EditorRequired]
    public IMisskeyPaginationSource<UserFollowRelationListItem> Pagination { get; set; } = default!;
}
