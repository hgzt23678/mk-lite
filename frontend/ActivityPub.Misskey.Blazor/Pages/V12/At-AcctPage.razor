@* The supported UserPage owns this route. This legacy placeholder is retained as
   source evidence only and deliberately has no route directive. *@

@code {
    [Parameter]
    public string? Acct { get; set; }
}

@code {
    [Parameter]
    public string? Page { get; set; }
}
