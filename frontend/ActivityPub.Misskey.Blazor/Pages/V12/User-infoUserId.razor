@* The supported UserInfoPage owns this route. This legacy file is retained as
   source evidence only and deliberately has no route directive. *@

@code {
    [Parameter]
    public string? UserId { get; set; }
}
